/** @type {import('next').NextConfig} */
const nextConfig = {
    // publicRuntimeConfig: {
    //     staticFolder: '/static',
    // },
    // assetPrefix: '',
    basePath: '',
    // output: 'export',
    distDir: 'build',
}

module.exports = nextConfig
